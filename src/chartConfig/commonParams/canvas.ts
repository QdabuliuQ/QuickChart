import { Common } from "../interface";

let canvas: Common = {
  name: '图表画布',
  opName: 'canvas',
  chartOption: false,
  menuOption: true,
  icon: 'i_canvas',
  componentPath: 'paramsCanvas.vue',
  defaultOption: {
    bgc: '#fff',
    repeat: 'repeat'
  },
}

export default canvas